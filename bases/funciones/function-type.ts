(() => {

    const addNumber = (a: number, b: number): number => a + b
    const saveWorld = (): string => 'World saved!!'
    const greed = (name: string): string => `Hello ${name}`

    //let myFunction: Function;
    let myFunction: (name: string) => string;

})()